FROM node:16

WORKDIR /usr/app

RUN npm install --global pm2

COPY ./package*.json ./

RUN npm install --production
RUN npm install

COPY ./ ./

RUN npm run build

EXPOSE 3000

USER node

FROM nginx:stable-alpine as production-stage
COPY  ./nginx/nginx.conf /etc/nginx/conf.d/default.conf

CMD [ "pm2-runtime", "start", "npm", "--", "start", "nginx"]
